import React from 'react';

const Setting = () => {
    return (
        <div className="container-fluid">
            <h1>
                출퇴근 설정
            </h1>
        </div>
    );
};

export default Setting;